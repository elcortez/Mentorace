<div class='container'>
  <h4><%= chapter.title %></h4>
  <h6><%= chapter.description %></h6>
  <% chapter.units.order(:position_in_chapter).each do |unit| %>
    <%= render partial: 'unit_display', locals: { unit: unit } %>
  <% end %>
</div>
